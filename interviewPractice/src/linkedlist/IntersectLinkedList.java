package linkedlist;

// 1. Given two linked lists, find the node at which they merge.  
// http://www.geeksforgeeks.org/write-a-function-to-get-the-intersection-point-of-two-linked-lists/
public class IntersectLinkedList {
	
	public Node findIntersect(Node x, Node y){
		
		// 1. get the length and tail of x, and y
		
		// 2. diff = x.length-y.length
		
		// 3. fast forward until reach x's diff node
		
		// 4. while loop until reaches the intersection
		
		
		return null;
	}
	
	
	// O(a) space
	// O(a+b) 
	public Node findIntersectHashMap(Node x, Node y){
		// 1. throw everything of x into a hashmap
		// 2. iterate over b
		
		return null;
	}
}
